# Revisión de constantes físicas de `autocalibracion_dos_fases.py`

Fecha: 2026-09-10. Archivo revisado: `src/interfaces/python/autocalibracion_dos_fases.py`, versión observada a las 19:03, SHA-256 `C572ABBBDD8817AED6CCE84006FC8C70EB60E16B694E629CC529B56843CF6461`.

No se abrió ningún puerto serie. La referencia de estado es `lab/HANDOFF_AUTOCALIBRACION_2026-09-10_TARDE.md`. La escala importada se rastreó además a `src/interfaces/python/escala_banco.py`. Riesgo: 5 = puede ordenar un movimiento contrario, tocar riel o declarar PASS fuera del requisito; 4 = puede llevar a un riel o a un modelo falso antes de terminar en FAIL; 3 = puede producir un falso inviable, perder autoridad o aceptar un transitorio; 2 = cambia reparto, tiempo o diagnóstico; 1 = no interviene en el hardware en la versión revisada.

La tabla cubre los números que representan tensión, corriente, resistencia, ganancia, pendiente, riel, ruido, tiempo de asentamiento, códigos de actuador y umbrales aplicados a esas magnitudes. Los índices de canal, cantidades de reintentos, versión de archivo y datos sintéticos de los tests se agrupan al final porque no describen la placa.

| constante o expresión | línea | de dónde sale | si el signo se invierte | si la magnitud yerra 70 % | riesgo |
|---|---:|---|---|---|---:|
| `margins[ch] <= 0,0` en la aceptación final, frente a `MARGEN_RIEL_MV = 250,0 mV` | 51; 163–172; 227–256 | El margen de `250,0 mV` está fijado en `HANDOFF_AUTOCALIBRACION_2026-09-10_TARDE.md:75–79`. `toca_riel` sí lo aplica; `evaluar_aceptacion` compara el margen físico con `0,0 mV`. Prueba sin hardware: con margen de `100,0 mV`, `toca_riel=True` y `acceptance.pass=True`. | Cambiar el umbral de `0,0` a un valor negativo amplía la aceptación. No hay validación que compare ambos criterios. | Un umbral efectivo de `75,0 mV` o `425,0 mV` cambia el requisito. En el código actual el error es del 100 % respecto de `250,0 mV`: usa `0,0 mV`. Puede declarar PASS entre el riel y la guarda. | 5 |
| `PASO_IDAC3_CH4_MV = +16,9`; `AUTORIDAD_IDAC3_MV = 255·16,9 = 4.309,5 mV`; cota `por_lpo` | 76–78; 133–160 | Es cálculo: `0,125 µA/código · 10 kΩ · (1 + 150 kΩ/12 kΩ) = 16,9 mV/código`. No hay ABBA vigente que lo sostenga. `RESULTADOS_MAPA_SUMADOR_2026-09-10.md:243–245,277` dice que el mapa no pudo medir LP y que falta medir `ch4/IDAC3` con ch4 vivo. Los archivos `polaridad_idac3_x1_minus32_20260908.json` y `...plus32...json` dan una comparación entre corridas de unos `+6,75 mV/código`, no un ABBA y no prueba `16,9`. | Un paso negativo vuelve negativa la autoridad, produce un objetivo ch2 negativo y además invierte la precompensación. No se rechaza el objetivo negativo. | Si el paso real es 70 % menor, la cota permite 3,33 veces el error que IDAC3 puede corregir; LPo puede rielar al conmutar. Si es 70 % mayor, se exige de más a IDAC2 o se declara inviable. El cálculo también supone la misma escala en ambos signos y los `255` códigos disponibles desde cualquier código actual. | 5 |
| `pre3 = round(-lpo_previsto/PASO_IDAC3_CH4_MV)` y luego `self.codes[3] + pre3` | 1342–1353 | Dirección algebraica basada en el paso teórico de IDAC3. No hay lectura de LPo ni guarda de riel entre este salto abierto y la conmutación; el ABBA de IDAC3 ocurre después, en 1361–1365. | Ordena IDAC3 hacia el lado contrario. Se detecta sólo después de conmutar, cuando ch4 puede estar clavado y el ABBA se rechaza. | Deja un residual proporcional al error del cociente. Con 70 % de error puede agotar IDAC3 o rielar LPo antes de que exista una pendiente medida. | 5 |
| `GANANCIA_LP_CH2_MV_POR_MV = -9,07` y duplicado `GANANCIA_LP_DESDE_CH2 = -9,07` | 73–75; 116; 145–159; 1342 | Mediana local informada en `HANDOFF_AUTOCALIBRACION_2026-09-10_TARDE.md:31–32`; `lab/calibracion_nueva/simulacion/analisis_csv_viejo.md:102,111` informa `-9,0699` con `R² = 0,9956`. El mapa vigente no pudo recalcularla: `RESULTADOS_MAPA_SUMADOR_2026-09-10.md:243–245`. | `por_lpo` usa valor absoluto y no lo nota; `lpo_previsto` conserva el signo y manda la precompensación al lado contrario. La falla aparece después de conmutar. | La cota de ch2 y la precompensación quedan erradas 70 %. No hay comprobación de LPo predicho antes de conmutar. | 5 |
| Espera de IDAC1: `ABBA_ESPERA_S = 1,5 s`, ABBA directo y verificación tras salto exclusivo de IDAC1 | 17–18; 120–122; 798–853; 868–869; 1046–1053 | El archivo afirma que IDAC1 es inmediato. El contexto autoritativo dice que mover IDAC0 **o IDAC1** exige `2 a 5 τ`: `HANDOFF...:51–59,84`. La corrida `dos_fases_calibrate-gains_20260910_185425.json:1374–1384` registró `+5,222 mV/código` para ch1 con este ABBA, frente al respaldo `+2,0`; esa diferencia no decide la planta porque la espera contradice el handoff. | El tiempo no tiene signo físico. Un valor negativo fallaría en `sleep`; no hay validación propia. | Con `0,45 s` aumenta el sesgo transitorio; con `2,55 s` sigue lejos de `2τ ≈ 59 s`. El optimizador y la verificación pueden usar el mismo transitorio y declarar fase 1 sobre un estado que luego cambia. | 5 |
| Asimetría IDAC0: `+89,5 mV/código` hacia negativos, `+51,5` hacia positivos; respaldo activo único `+49,7` y `-197,3` en ch2 | 93–115; 310–336; 866–899 | La asimetría y cuatro resultados (`85,0`, `90,1`, `49,7`, `52,4`) están anotados sólo en el propio archivo, líneas 93–109; no se encontró allí el nombre de un archivo de `lab/` con las cuatro mediciones. `HANDOFF...:122–133` sí registra `49,7` y `85,0`. Las constantes por rama de 110–111 no tienen usos; los respaldos de 112 y 114 sí. | La pendiente directa invertida es descartada por `pendiente_directa_utilizable`; entra el respaldo positivo. La pendiente de ch2 equivocada manda optimización y rescate al lado contrario. El rescate aborta tras verificar un salto; la optimización puede llegar al riel antes de verificar. | En la rama negativa el respaldo de `49,7` subestima alrededor de 44 % respecto de `89,5`; expresado contra la rama menor, la asimetría es 74 %. Puede pedir casi el doble de códigos, y el recálculo no sirve si el salto deja el tap inválido o clavado. Las constantes por rama no corrigen este camino porque no se consultan. | 5 |
| `pendiente_directa_utilizable`: exige pendiente `> 0,0` y luego usa respaldo si no se cumple | 340–343; 878–899 | Es una premisa escrita en código. Las pendientes observadas en esta placa son positivas para ch0 e ch1, pero no hay evidencia de varias placas; `HANDOFF...:130–133,144–150` limita la evidencia a una placa. | Una inversión física real se mide, pero el programa la clasifica como no utilizable y sustituye una constante de signo positivo. No queda una alarma específica de polaridad. | Un error de magnitud no se rechaza mientras supere `1 µV de banco/código`; se usa en el primer salto. El recálculo puede corregirlo sólo si ambos extremos siguen vivos. | 5 |
| Direcciones `raw = -error/slope`, `before + correction`, `idac2_needed = round(-error/5,1)` y `code2 = round(-predicted_error/5,1)` | 263–273; 477; 515; 940–954; 1057–1065; 1094–1105; 1142–1189 | Son las ecuaciones de corrección. Las unidades coinciden en cada uso: banco µV con banco µV/código en fase 2; mV físicos con mV/código en fase 1. | Con pendiente de signo contrario todos los `+=` implícitos por `before + delta/correction` van al lado contrario. El lazo PI no aborta al primer empeoramiento; conserva el mejor punto y lo restaura al final. El rescate sí verifica acercamiento tras un salto. | El primer salto PI está limitado a `96` códigos y los siguientes a `4`; un error de 70 % puede provocar sobrepaso, ciclos o límite. La verificación final evita un PASS si llega a riel, salvo por el defecto del umbral de `250,0 mV`. | 5 |
| Conversión `FACTOR = 19,9157`, `TARGET = 1001,5 mV banco`, ventana `880,4…1122,7 mV banco` | 36–45; 125–130; 168–180 | `escala_banco.py:3–28,46–61` la deriva de dos puntos con tester del 2026-09-05. `RESULTADOS_MAPA_SUMADOR_2026-09-10.md:283–287` reutiliza la misma escala. El handoff actual dice Vref `2,48 V` con Vdda `4,96 V`, mientras la calibración de escala fija Vref `2,415 V` y Vdda `4,826 V`: `HANDOFF...:35–38` y `escala_banco.py:51–61`. Falta una lectura simultánea banco/tester del Vref actual. | Un factor negativo vuelve negativos los márgenes físicos y produce FAIL; también invierte las conversiones de pendiente. La incoherencia se detecta en margen, no con un mensaje de escala. | El error de 70 % cambia todos los umbrales expresados en mV físicos. Parte del error cancela al convertir pendiente medida ida y vuelta, pero no cancela en márgenes, respaldos escritos en mV ni objetivo. | 4 |
| `GANANCIA_SUM_DESDE_BPO = 27/7,48 = 3,610`; respaldo `d(ch2)/d(IDAC1) = -8,3` | 64–70; 113–115; 319–324; 375–380 | `HANDOFF...:23–32` da `b = 4,16` por ABBA y `d(ch2)/d(IDAC1) = -7,61` en 52 tramos en 99–109. El código usa la relación de resistencias `3,610`; su comentario combina además `2,0·3,610 = 7,22`, cerca de `7,61` por compensación entre dos números distintos. | El signo inversor está aplicado en 319–320 y 380. Si se quita, el test 1668–1681 lo detecta; si el hardware invierte, la derivación y el test comparten la misma premisa y el salto va al lado contrario. | Un error de 70 % en `b` mueve ch2 70 % distinto de la predicción. El salto realizado sólo recalcula desde ch1 si ch1 permanece vivo; el respaldo `-8,3` evita la derivación de `3,610` únicamente cuando falla el ABBA directo. | 4 |
| `PASO_IDAC2_CH2_MV = +5,1 mV/código` | 72; 477; 515; 1057; 1094; 1307–1313 | ABBA y mapa: `HANDOFF...:11–12,23–33,40–46`; `RESULTADOS_MAPA_SUMADOR_2026-09-10.md:113–125,193–212,231–240`. Se vuelve a medir por ganancia; la constante entra sólo si el ABBA resulta no utilizable y también en la planificación de fase 1. | En fase 2 el ABBA normal conservaría el signo medido. En fallback, el signo contrario mueve IDAC2 al lado incorrecto; el PI puede visitar el riel antes de restaurar su mejor punto. | La planificación de fase 1 calcula mal la reserva y el objetivo por códigos. El PI realimenta y puede recuperarse, con más iteraciones; si toca riel, la lectura deja de servir. | 4 |
| Ganancia nominal `G` de `GAIN_CODES = 1,2,4,8,16,24,32,48,50` usada como ganancia física exacta | 46–47; 60–61; 137–160; 1203–1208; 1256–1261 | La tabla está en `testbench/core/lab.py:43–44`; el rango comprometido y de mejor esfuerzo está en `HANDOFF...:75–85`. No se encontró una medición de ganancia real por código en el handoff. | Una ganancia negativa no está representada por `GAIN_CODES`; no se detectaría como tal porque sólo se verifica el ACK del código. | Si la ganancia real supera 70 % la nominal, `_prediction_sum` subestima media y pico a pico y puede autorizar una conmutación que riela SUMo. Si es menor, aborta o ajusta de más. La lectura posterior detecta el riel, después de conmutar. | 4 |
| Umbral `PP_CLAVADO_UV = 300,0 µV banco`, importado mediante `clavado()` | 166; 203–205; 236–237; 666–689; 804–816 | `escala_banco.py:68–90` lo fija entre máximo clavado `190 µV` y mínimo vivo `514 µV`, sobre 217 lecturas; `HANDOFF...:13–14` cita `114 µV` frente a `1125–3166 µV`. | Un umbral negativo no marca ningún tap como clavado; se permitiría ABBA sobre un riel y podría haber PASS. | A `90,0 µV` puede tratar como vivos los rieles observados de `114–190 µV`; a `510,0 µV` queda a `4 µV` del mínimo vivo citado y puede generar falsos clavados. | 4 |
| `TAU_BP_REFERENCIA_S = 29,5 s`; espera `2,0…5,0 τ`; adquisición `3,2·29,5 s` | 118–120; 739–795 | `HANDOFF...:18–21,33–34,51–59,84` da `τ ≈ 29,5 s`, diseño `43 kΩ·680 µF`, y exige `2 a 5 τ`. El código anota mediciones `24,9…42,3 s`; la corrida `dos_fases_calibrate-gains_20260910_185425.json` registró `34,6 s`. `PREGUNTAS_PARA_ELIAS.md:19–20` estima unos `40 s` en temperatura. | τ negativo hace fallar `sleep` o el timeout. No hay validación explícita de `tau > 0`. | Si el fallback queda 70 % bajo, la espera mínima es `17,7 s`, sólo `0,6 τ` respecto de `29,5 s`; se puede verificar un transitorio. Si queda 70 % alto, conserva dirección pero consume tiempo y presupuesto. El comentario “conservadora” no vale para el extremo de `42,3 s`. | 4 |
| Riel bajo usado: `BANCO_MIN_VALIDO_MV = 880,4` equivalente a `0 V`; riel alto usado para todos: `min(1122,7; 1106,1)` | 36–45; 168–180; 932–935 | La ventana ADC está en `escala_banco.py:55–61`; el riel `1106,1 mV banco = 4,50 V` es de LPo, `escala_banco.py:67–76` y `HANDOFF...:35–36`. El handoff da riel bajo de LPo `0,040 V`, no `0 V`. No hay riel alto y bajo por cada uno de los cinco taps. | Invertir cuál límite es alto/bajo vuelve negativos los márgenes y produce FAIL. | Un error de 70 % en la distancia de un riel puede aceptar una excursión fuera del margen o rechazar una válida. En el dato disponible, usar `0 V` en vez de `0,040 V` sobrestima el margen bajo en unos `40 mV`; aplicar el riel de LPo a los otros taps puede ser conservador o no, y hace falta barrer cada tap. | 4 |
| `MARGEN_FASE1_MV = 1.250,0 mV` | 51–59; 421–443; 1055–1065; 1092–1105 | `HANDOFF...:75–83` exige más de `1.000 mV` y dice `1.500 mV` deseable. El valor `1.250 mV` se justifica sólo en comentarios del código mediante un caso de reposo y reservas de `58 %`, `35 %` y `4 %`; no cita un archivo de `lab/`. | Un margen negativo vuelve factibles posiciones junto o fuera del riel; la verificación repite el mismo umbral y puede declarar fase 1 PASS. | `375 mV` incumple el requisito de `1.000 mV`; `2.125 mV` puede declarar sin punto factible aunque el requisito tenga solución. | 4 |
| `PASO_IDAC1_CH1_MV = +2,0 mV/código` | 13; 67–68; 113; 311–320; 758; 868–899 | `HANDOFF...:23–31` lo da como ABBA. `RESULTADOS_MAPA_SUMADOR_2026-09-10.md:94–100` mide `+1,8833` y `+1,8290 mV/código` en dos pasadas. La medición actual de `+5,222` usa la espera de `1,5 s` discutida arriba y no decide el valor asentado. | Una medición negativa se descarta y se reemplaza por `+2,0`; el signo físico contrario termina convertido en una orden contraria. | El optimizador mueve IDAC1 70 % de más o de menos. El recálculo desde el salto puede corregirlo si ch1 queda vivo y si se esperó el estado asentado. | 4 |
| `SWING_LPO_MV = 1.750,0 mV`; `por_sumo = (1.750−250)/G` | 51; 62–63; 137–160 | El código dice `1,75 V` útiles y vuelve a restar `250 mV`. `HANDOFF...:61–73` escribe `(2.000−250)/G`, luego dice `1.500 mV` en x1 y `62 mV` en x24; fórmula y resultados no coinciden entre sí. En la versión revisada `por_lpo` domina según el propio código, por lo que esta cota no decide mientras las otras constantes no cambien. | Un swing negativo produce objetivo negativo si esta rama llega a dominar; no hay validación del resultado. | Si queda 70 % alto puede permitir una conmutación que riela SUMo; `_prediction_sum` vuelve a comprobar con riel medido y puede abortar. Si queda bajo, ajusta de más o da un falso inviable. Hace falta fijar si la base es `2.000`, `1.750` o `1.500 mV` y dónde se descuenta la guarda. | 3 |
| `GANANCIA_SUM_DESDE_SEO = 27/6,8 = 3,9706` y signo `-` al derivar ch2 | 64–69; 319; 323; 375–380 | Relación de resistencias del esquemático: `HANDOFF...:23–27,122–133`. El handoff indica que estas relaciones se usan para derivar ch2 y son estables entre placas. | Quitar el `-` manda fase 1 al lado contrario; el test 1668–1681 lo detecta. Si el hardware contradice el signo, el test no es independiente porque construye el modelo con la misma fórmula. | Un error de 70 % en el cociente altera optimización y rescate. El salto realizado recalcula la pendiente directa ch0, pero no el cociente: vuelve a multiplicar por el mismo `27/6,8`. | 3 |
| `MARGEN_MAXIMO_MV = 2.230,0 mV` | 52–58; 79–82; 486–490; 524–528 | Mitad de `4,50−0,040 V`, coherente con `HANDOFF...:35–36,80–81`. Normaliza margen frente a reserva de códigos; no es una compuerta. | Negativo invierte la contribución de margen al reparto y puede seleccionar el margen menor; no hay validación. | Cambia el peso relativo entre margen y reserva de IDAC en el desempate. Las restricciones de `1.250 mV` se verifican aparte, pero puede elegirse un punto con menos reserva. | 3 |
| Rango firmado `IDAC_MIN, IDAC_MAX = -255, +255` | 119; 305; 421–490; 656–668 | Firmware: `src/firmware/psoc/AcondicionamientoAnalogico.cydsn/psoc_hw.h:114–147`; el handoff anterior citado por el actual también documenta el dominio, pero el estado vigente se confirma por el archivo de firmware. | Intercambiar signos invalida los rangos y las saturaciones de software; falla antes de controlar. | Con `±77` se pierde autoridad; con `±434` se ordenan códigos fuera del rango físico y se depende del rechazo o clamp del firmware. | 3 |
| Excitación de τ: `4` códigos para IDAC0, `8` para IDAC1; cálculo `49,7·1,09·4 ≈ 216 mV` | 750–771 | Derivación escrita en el código; `-R5/R4 = -1,09` no tiene archivo de `lab/` citado. Usa el paso único de IDAC0 que ya contradice la rama negativa. | El signo del salto se elige con `error·known_slope`; un signo falso excita hacia el riel. Sólo se limita el código, no el margen predicho del tap. | Con 70 % menos excursión puede caer bajo el umbral `8·ruido`; con 70 % más puede acercarse al riel. La medición se rechaza si el ajuste no observa amplitud, pero el salto ya ocurrió. | 3 |
| Criterio de estabilidad: `max(pp anterior, pp actual, 1,0 mV real)/2`, `3` consecutivas cada `3,0 s`; para BP, `2` consecutivas cada `max(2,0; min(5,0; 0,15τ))` | 717–744 | Es un criterio del código. No se encontró calibración de estos números en `lab/`; usa el pp medido como resolución. | Los tiempos o el número requerido negativos producen error o aceptación inmediata según el campo; no hay validación. | Un umbral 70 % mayor puede aceptar deriva; uno menor puede agotar `180 s`. En `_wait_bp` siempre se duerme primero `2τ`, por lo que el riesgo se concentra en un τ errado o en el caso IDAC1 que no llama a `_wait_bp`. | 3 |
| Ajuste de τ: mínimo `8` muestras, ruido en últimas `5`, búsqueda `2,0…120,0 s`, amplitud `max(8·ruido, 2,0 mV real)`, `R² >= 0,70` | 547–580 | Son umbrales del código; no se encontró un estudio de sensibilidad en `lab/`. Las unidades sí coinciden: amplitud y ruido están en µV de banco, y `bank_uv(2,0)` convierte el piso físico a esa unidad. | El signo de la amplitud se elimina con `abs`, lo que corresponde a τ; un τ negativo no está en la grilla. | Puede aceptar un ajuste sesgado o caer al fallback de `29,5 s`. Un 70 % en la grilla aún cubre el τ observado; un 70 % en amplitud o `R²` cambia qué corridas usan fallback. | 3 |
| ABBA: deltas de fase 1 `(2,4)` y de fase 2 `(8,4)` códigos; espera `1,5 s` | 798–853; 868–869; 1307; 1361 | El handoff exige ABBA en lugar de barridos monótonos: `HANDOFF...:18–21`. No fija esos deltas. Para IDAC0, medir una sola dirección desde cero evita promediar ramas, pero no modela un salto que cruce cero. | El cociente `(B−A)/delta` conserva el signo; el problema aparece si el signo se descarta después en fase 1. | Con deltas 70 % menores domina ruido y cuantización; con deltas mayores puede tocar riel o cruzar de rama. El precheck sólo evalúa el origen, no el punto B antes de escribirlo. | 3 |
| `MEJORA_MINIMA_RECURSO = 0,15`; casos comentados `0,05`, `0,16 V`, reservas `7 %`, `58 %`, `35 %`, `4 %` | 55–58; 83–89; 464–483; 532–538 | Ajuste sobre tres casos descritos en comentarios. No cita archivos de `lab/`; no es una constante de la planta, pero decide si se paga un τ. | Negativo hace que cualquier mejora ordene movimiento; no hay validación. | A `0,045` aumenta movimientos lentos; a `0,255` puede conservar un punto con menos reserva. Las restricciones físicas se comprueban aparte. | 2 |
| `GANANCIA_NO_INVERSORA = 8,58` y `R_IDAC2_KOHM = 5,1` duplicadas | 10; 15; 71; 117 | `HANDOFF...:23–34` sostiene ambas; el óhmetro dio `4,9 kΩ`. | Sin efecto: ninguna de las dos constantes se usa en una expresión ejecutada. | Sin efecto en esta versión; pueden desinformar una modificación posterior si quedan sin vínculo con la medición. | 1 |
| ADC `±2,5 V`, PGA x50, PGAout x1; lectura DC con selector `2 = 120 ms` | 46–47; 677; 696–703 | La configuración está documentada en `testbench/core/lab.py:35–44,237–249` y se usa en los mapas, por ejemplo `RESULTADOS_MAPA_SUMADOR_2026-09-10.md:7–10`. | No aplica un signo a los códigos de configuración; una polaridad analógica invertida no se comprueba aquí. | Un rango o ganancia física 70 % distinto cambia saturación y ruido. La ventana y `clavado()` pueden detectarlo, pero el ACK sólo confirma el código, no la ganancia real. | 2 |
| `OTRAS_ETAPAS_ESPERA_S = 30,0 s`; timeout general `180,0 s`; presupuesto `600,0 s`; pausas de comunicación `0,1`, `0,2`, `0,25 s`; apertura `25,0 s` | 121; 568–583; 638–658; 717–737; 1131–1140; 1414–1424; 1828; 1851 | El handoff sólo dice “decenas de segundos” para otras etapas y `2 a 5τ` para BP: `HANDOFF...:84`. Los demás son parámetros operativos sin medición citada. | Un tiempo negativo genera error en `sleep` o timeout; no invierte una corrección. | Un 70 % menor puede verificar antes del asentamiento; un 70 % mayor consume presupuesto. `_wait_fast` agota el timeout y de todos modos devuelve una lectura, por lo que no convierte falta de estabilidad en FAIL. | 2 |
| Umbral de pendiente `1,0 µV banco/código` y parada `0,5 código` | 263–270; 340–343; 1308–1313; 1362–1365 | Son umbrales de resolución del código. En `correccion_discreta` la pendiente está en µV banco/código; en `pendiente_directa_utilizable` se convierte desde mV reales/código antes de comparar. Las unidades coinciden. No hay archivo de `lab/` que ajuste el `1,0`. | El umbral usa valor absoluto; el signo de fase 2 se conserva. En fase 1 hay además el filtro positivo ya señalado. | Con 70 % cambia sólo la decisión entre medir y usar fallback; `0,5 código` es la frontera de redondeo, no una constante física de placa. | 2 |
| Números descriptivos no ejecutados: márgenes `1,0`, `1,5`, `2,0`, `2,23 V`; ejemplo `+1.204 mV`, `−10,9 V`, factor `2,6`; ejemplo de precompensación `1,93/2,0 V`, `96 %` | 52–58; 145–153; 1264–1269; 1329–1341 | Son comentarios que explican decisiones. Parte coincide con `HANDOFF...:61–83`; el caso `−96,3 → +1.204 mV` no cita el JSON en el comentario. | No cambian órdenes. Un signo falso en el comentario puede ocultar una premisa falsa, pero no se ejecuta. | No cambian el resultado. Deben actualizarse junto con las constantes para no validar a mano con una cuenta que comparta la premisa. | 1 |
| Magnitudes sintéticas de tests: `50,0/85,0/2,0 mV/código`, errores `−2.500…+2.200 mV`, pp `100…3.000`, pendiente `10,0 µV/código`, cambio `850 mV/10 códigos` | 1476–1798 | Son plantas y puntos sintéticos de `Autotest`, no mediciones. El caso `50→85` reproduce el error de 70 % por construcción. | Los tests de signo usan la misma fórmula de sumador que producción; detectan un cambio de código, no una contradicción independiente del hardware. | El test de 70 % demuestra que el recálculo funciona cuando ambos extremos siguen válidos; no cubre que el primer salto riele, el cruce de cero, el fallback por rama ni la aceptación a menos de `250 mV`. | 1 |

## Cambios sustentados por código y evidencia

1. Hacer que `evaluar_aceptacion` aplique `MARGEN_RIEL_MV` a los cinco taps, igual que `toca_riel`, y agregar una prueba con un tap vivo a `100,0 mV` del riel. Es un bug: el requisito es `250,0 mV` y la línea 235 usa `0,0 mV`.
2. No usar `PASO_IDAC0_CH0_MV = 49,7` ni `PASO_IDAC0_CH2_MV = -197,3` como respaldo independiente del signo. Las ramas medidas ya están separadas en 110–111, pero hoy no participan del modelo. El recálculo posterior no cubre rescate, primer salto inválido ni cruce de cero.
3. No descartar una pendiente directa por ser negativa. Tratarla como contradicción de polaridad, abortar antes de mover y registrar la evidencia; no sustituirla por un respaldo de signo opuesto.
4. Resolver la contradicción temporal de IDAC1. Mientras el handoff exija `2 a 5τ`, no usar `1,5 s` para identificar ch1 ni para verificar un salto exclusivo de IDAC1.
5. Separar la prueba de signo de la fórmula que se prueba. Los tests actuales prueban que el código incluye un `-`, pero no comparan una orden con un dato de hardware independiente.

## Cambios que necesitan medición

1. Medir `d(ch4)/d(IDAC3)` con ch4 vivo mediante ABBA separado en códigos negativos y positivos, alrededor del código de operación y antes de usar `16,9 mV/código`, `4.309,5 mV` de autoridad o `por_lpo`. Registrar también autoridad restante desde códigos IDAC3 distintos de cero. Hasta entonces, la cota por LPo y la precompensación abierta no tienen respaldo de placa.
2. Medir las dos ramas firmadas de IDAC1, IDAC2 e IDAC3. El mismo `polarity_reg` gobierna los cuatro IDAC (`psoc_hw.h:125–147`); la asimetría sólo está cuantificada para IDAC0.
3. Medir en un mismo ensayo asentado `d(ch1)/d(IDAC1)` y `d(ch2)/d(IDAC1)` para decidir entre `27/7,48 = 3,610`, `b = 4,16` y el respaldo `-8,3 mV/código`. No aceptar la cercanía `7,22` contra `7,61` como validación porque combina dos errores que se compensan.
4. Repetir la recta banco→físico y el punto de Vref con la alimentación actual: dos puntos separados con tester, Vdda, Vref y lectura de banco simultáneos. Decidir después si `TARGET = 1001,5` sigue representando el Vref de `2,48 V`.
5. Medir riel bajo y alto, con pp, para cada tap. Hoy se usa `0 V` como riel bajo y el riel alto de LPo para los cinco canales.
6. Medir la ganancia real de PGAout para cada código comprometido y usarla en `_prediction_sum`, o justificar con tolerancia por qué el nominal protege el margen de `250,0 mV`.
7. Fijar con una sola derivación el swing de SUMo: `2.000−250`, `1.750−250` o `1.500 mV`. El handoff y el código contienen las tres cifras sin una ecuación consistente.

## Verificaciones sin hardware

- `py -3 src/interfaces/python/autocalibracion_dos_fases.py --autotest`: 15 de 15 tests pasaron.
- Caso de borde construido en memoria: margen físico `99,997 mV`; `toca_riel(...)=True`; `evaluar_aceptacion(...)["pass"]=True`.
- No se abrió COM8 ni otro puerto.
