# Claude Code — sesión ephemera (no compartir)
.claude/scheduled_tasks.lock
.claude/__pycache__/

# Skills, settings y configuraciones → sí versionar
# (quitar las líneas anteriores del ignorado si se desea excluir todo .claude)
